<h2>All Genres:</h2>

<% @genres.each do |genre| %>
  <%= genre.name %>
<% end %>

<h3>Create A New Genre:</h3>

<%= form_for [:admin, @genre] do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "Submit" %>

<% end %>
