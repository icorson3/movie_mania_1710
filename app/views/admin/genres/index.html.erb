
Hello

<%= form_for [:admin, @genre], url: admin_genres_path do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit %>
<% end %>

<% @genres.each do |genre| %>
  <%= genre.name %>
<% end %>
