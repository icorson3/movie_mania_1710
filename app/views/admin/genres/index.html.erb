<h1> Genres</h1>

<%= form_for [:admin, @genre] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit %>
<% end %>

<h1> All Genres</h1>

<% @genres.each do |genre|  %>
Name: <%= genre.name %>
<% end %>
