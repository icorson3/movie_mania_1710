<div class="movie-index">
  <h1>All Movies</h1>

  <% @movies.each do |movie| %>
    <%= link_to "#{movie.title}", movie_path(movie.slug) %>
    <p><%= movie.description %></p>
    <%= button_to "Add Movie", carts_path(movie_id: movie.id), class: 'cart-button' %>
  <% end %>
</div>
